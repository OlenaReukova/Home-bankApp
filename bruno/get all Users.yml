info:
  name: get all Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/bank/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
