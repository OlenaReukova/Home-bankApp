info:
  name: create User with X-Correlation-ID
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/bank/users"
  headers:
    - name: X-Correlation-ID
      value: my-test-id-123
  body:
    type: json
    data: |-
      {
        "firstName": "Ned",
        "lastName": "Good",
        "email": "ned_102@gmail.com",
        "phoneNumber": "0712345345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
