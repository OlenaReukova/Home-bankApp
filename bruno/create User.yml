info:
  name: create User
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/bank/users"
  body:
    type: json
    data: |-
      {
        "firstName": "Tom",
        "lastName": "James",
        "email": "tom_102@gmail.com",
        "phoneNumber": "0712345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
