info:
  name: particial update User
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/bank/users/{{id}}"
  body:
    type: json
    data: |-
      {
        "firstName": "Tom update",
        "lastName": "James",
        "email": "tom_102@gmail.com",
        "phoneNumber": "0712345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
