info:
  name: delete User
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/bank/users/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
