info:
  name: actuator-health-readiness
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/actuator/health/readiness"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
