info:
  name: actuator-health-liveness
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/actuator/health/liveness"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
