info:
  name: update User
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/bank/users/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
